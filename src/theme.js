import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: '#004d40',
    },
    secondary: {
      main: '#e64a19',
    },
  },
});

export default theme;